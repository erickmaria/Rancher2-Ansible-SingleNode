- hosts: server
  become: yes
  tasks:

    - name: "Intall Rancher 2.2.6"
      command: docker run -d --restart=unless-stopped \
                 -p 80:80 -p 443:443 \
                 rancher/rancher:v2.2.6
